<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys" xmlns:dxeh="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal">

    <ResourceDictionary.MergedDictionaries>
        <dxdst:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Seven.v15.2;component/DevExpress.Xpf.Core/DevExpress.Xpf.Core/Themes/StandardControls/Brushes.xaml" />
        <dxdst:ResourceDictionaryEx Source="/DevExpress.Xpf.Themes.Seven.v15.2;component/DevExpress.Xpf.Core/DevExpress.Xpf.Core/Themes/StandardControls/SharedResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="{dxdst:DefaultStyleThemeKey FullName=System.Windows.Controls.RadioButton}" TargetType="{x:Type RadioButton}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Foreground}" />
        <Setter Property="Background" Value="{StaticResource Button.Background}" />
        <Setter Property="BorderBrush" Value="{StaticResource Button.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0,3,0,3" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="dxeh:ControlHelper.ShowFocusedState" Value="{Binding Path=IsFocused, RelativeSource={RelativeSource Self}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <ControlTemplate.Resources> 
                        <Storyboard x:Key="StoryboardHover.Enter">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="HoverState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="1" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardHover.Exit">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="HoverState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardPressed.Enter">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="PressedState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="1" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardPressed.Exit">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="PressedState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardChecked.Enter">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="1" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardChecked.Exit">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardIconHover.Enter">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon_HoverState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="1" />
                            </DoubleAnimationUsingKeyFrames>

                        </Storyboard>
                        <Storyboard x:Key="StoryboardIconHover.Exit">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon_HoverState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardIconPressed.Enter">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon_PressedState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="1" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="StoryboardIconPressed.Exit">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity" Storyboard.TargetName="RadioButtonIcon_PressedState">
                                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid Background="{TemplateBinding Background}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Top" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="InternalFocusStates">
                                <VisualState x:Name="InternalFocused">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusedState" Storyboard.TargetProperty="Opacity" Duration="0" To="1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="InternalUnfocused" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid Width="14" Height="14" Margin="0,1,0,1" Grid.Column="0" Grid.ColumnSpan="1">
                                <Grid x:Name="FocusedState" Opacity="0">
                                    <Border Style="{StaticResource RadioButtonFocusTemplate}" />
                                </Grid>
                                <Grid x:Name="DefaultState">
                                    <Border Background="{StaticResource RadioButton.DefaultState.Border2}" BorderBrush="{StaticResource RadioButton.DefaultState.Border1}" BorderThickness="1" CornerRadius="64">
                                        <Border Background="{StaticResource RadioButton.DefaultState.Border4}" BorderBrush="{StaticResource RadioButton.DefaultState.Border3}" BorderThickness="1" Margin="1" CornerRadius="64" />
                                    </Border>
                                </Grid>
                                <Grid x:Name="HoverState" Opacity="0">
                                    <Border Background="{StaticResource RadioButton.HoverState.Border2}" BorderBrush="{StaticResource RadioButton.HoverState.Border1}" BorderThickness="1" CornerRadius="64">
                                        <Border Background="{StaticResource RadioButton.HoverState.Border4}" BorderBrush="{StaticResource RadioButton.HoverState.Border3}" BorderThickness="1" Margin="1" CornerRadius="64" />
                                    </Border>
                                </Grid>
                                <Grid x:Name="PressedState" Opacity="0">
                                    <Border Background="{StaticResource RadioButton.PressedState.Border2}" BorderBrush="{StaticResource RadioButton.PressedState.Border1}" BorderThickness="1" CornerRadius="64">
                                        <Border Background="{StaticResource RadioButton.PressedState.Border4}" BorderBrush="{StaticResource RadioButton.PressedState.Border3}" BorderThickness="1" Margin="1" CornerRadius="64" />
                                    </Border>
                                </Grid>
                                <Grid x:Name="DisabledState" Opacity="0">
                                    <Border Background="{StaticResource RadioButton.DisabledState.Border2}" BorderBrush="{StaticResource RadioButton.DisabledState.Border1}" BorderThickness="1" CornerRadius="64" />
                                </Grid>
                                <Grid x:Name="RadioButtonIcon" Opacity="0">
                                    <Border UseLayoutRounding="True" Margin="2" Background="#26294962" CornerRadius="64">
                                        <Grid>
                                            <Grid x:Name="RadioButtonIcon_DefaultState">
                                                <Border UseLayoutRounding="True" Margin="1" Background="{StaticResource RadioButton.DefaultState.Icon.Border1}" CornerRadius="64">
                                                    <Border Background="{StaticResource RadioButton.DefaultState.Icon.Border2}" Margin="1" CornerRadius="64" />
                                                </Border>
                                            </Grid>
                                            <Grid x:Name="RadioButtonIcon_HoverState" Opacity="0">
                                                <Border Background="{StaticResource RadioButton.HoverState.Icon.Border1}" CornerRadius="64">
                                                    <Border Background="{StaticResource RadioButton.HoverState.Icon.Border2}" Margin="1" CornerRadius="64" />
                                                </Border>
                                            </Grid>
                                            <Grid x:Name="RadioButtonIcon_PressedState" Opacity="0">
                                                <Border Background="{StaticResource RadioButton.PressedState.Icon.Border1}" CornerRadius="64">
                                                    <Border Background="{StaticResource RadioButton.PressedState.Icon.Border2}" Margin="1" CornerRadius="64" />
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>
                            <ContentPresenter Margin="4,0,0,0" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Grid.Column="1" Grid.ColumnSpan="1" RecognizesAccessKey="True" />
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="RadioButtonIcon" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardHoverExit_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardHoverEnter_BeginStoryboard" Storyboard="{StaticResource StoryboardHover.Enter}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardHoverEnter_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardHoverExit_BeginStoryboard" Storyboard="{StaticResource StoryboardHover.Exit}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardPressedExit_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardPressedEnter_BeginStoryboard" Storyboard="{StaticResource StoryboardPressed.Enter}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardPressedEnter_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardPressedExit_BeginStoryboard" Storyboard="{StaticResource StoryboardPressed.Exit}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardIconHoverExit_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardIconHoverEnter_BeginStoryboard" Storyboard="{StaticResource StoryboardHover.Enter}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardIconHoverEnter_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardIconHoverExit_BeginStoryboard" Storyboard="{StaticResource StoryboardHover.Exit}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardIconPressedExit_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardIconPressedEnter_BeginStoryboard" Storyboard="{StaticResource StoryboardPressed.Enter}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <!--<RemoveStoryboard BeginStoryboardName="StoryboardIconPressedEnter_BeginStoryboard" />-->
                                <BeginStoryboard x:Name="StoryboardIconPressedExit_BeginStoryboard" Storyboard="{StaticResource StoryboardPressed.Exit}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledState" Property="Opacity" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>