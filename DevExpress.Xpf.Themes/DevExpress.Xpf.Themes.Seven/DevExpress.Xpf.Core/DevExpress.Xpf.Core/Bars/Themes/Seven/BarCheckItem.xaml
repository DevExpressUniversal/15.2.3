<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
                    xmlns:dxbt="http://schemas.devexpress.com/winfx/2008/xaml/bars/themekeys">

    <Thickness x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=BorderMarginInMenu}">-1,1,-1,1</Thickness>

    <ControlTemplate x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=CheckInMenu}" TargetType="{x:Type ContentControl}">
        <Border x:Name="Root" Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16">
            <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                <Path Stretch="Fill" Fill="#FF15273F"
                      Data="F1 M 399,86L 403,93L 409,81L 403,90L 399,86 Z"
                      Height="12" Width="10" UseLayoutRounding="True"/>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="FlowDirection" Value="RightToLeft">
                <Setter TargetName="Root" Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleY="1" ScaleX="-1"/>
                    </Setter.Value>
                </Setter>
            </Trigger>         
        </ControlTemplate.Triggers>         
    </ControlTemplate>

    <ControlTemplate x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=GlyphCheckBorderInMenu}" TargetType="{x:Type ContentControl}">
        <Border Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16">
            <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                <ContentPresenter/>
            </Border>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=GlyphTriStateBorderInMenu}" TargetType="{x:Type ContentControl}">
        <Border Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16">
            <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                <ContentPresenter/>
            </Border>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=TriStateBorderInMenu}" TargetType="{x:Type ContentControl}">
        <Border x:Name="Root" Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16">
            <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                <Path Stretch="Fill" Fill="#FF15273F"
                      Data="F1 M 399,86L 403,93L 409,81L 403,90L 399,86 Z"
                      Height="12" Width="10" UseLayoutRounding="True"/>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="FlowDirection" Value="RightToLeft">
                <Setter TargetName="Root" Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleY="1" ScaleX="-1"/>
                    </Setter.Value>
                </Setter>
            </Trigger>         
        </ControlTemplate.Triggers>         
    </ControlTemplate>

    <Style x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=TextSplitterStyleInRibbonPageGroup}" TargetType="{x:Type dxb:TextSplitterControl}">
        <Setter Property="FirstStringHorizontalAlignment" Value="Center"/>
        <Setter Property="SecondStringHorizontalAlignment" Value="Center"/>
        <Setter Property="SecondStringMargin" Value="0,-3,0,0"/>
        <Setter Property="Margin" Value="0,-2,0,0"/>
    </Style>
    
    <ControlTemplate x:Key="CheckTriStateTemplate">
        <Grid x:Name="Root" RenderTransformOrigin="0.5,0.5">            
            <Border x:Name="Path1" Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16" Visibility="{Binding Path=(dxb:BarCheckItemLinkControlStatesProvider.CheckVisibility), RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                    <Path Stretch="Fill" Fill="#FF15273F"
                          Data="F1 M 399,86L 403,93L 409,81L 403,90L 399,86 Z"
                          Height="12" Width="10" UseLayoutRounding="True"/>
                </Border> 
            </Border>    
            <Border Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16" Visibility="{Binding Path=(dxb:BarCheckItemLinkControlStatesProvider.GlyphCheckVisibility), RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                    <ContentPresenter/>
                </Border> 
            </Border>    
            <Border Background="#FFACACAC" CornerRadius="3" MinWidth="16" MinHeight="16" Opacity="0.6" Visibility="{Binding Path=(dxb:BarCheckItemLinkControlStatesProvider.GlyphTriStateVisibility), RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="#FFD6D6D6" Margin="1" CornerRadius="2">
                    <ContentPresenter/>
                </Border> 
            </Border>    
            <Border x:Name="Path2" Background="#FFACBCCD" CornerRadius="3" MinWidth="16" MinHeight="16" Opacity="0.6" Visibility="{Binding Path=(dxb:BarCheckItemLinkControlStatesProvider.TriStateVisibility), RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="#FFD6E4F8" Margin="1" CornerRadius="2">
                    <Path Stretch="Fill" Fill="#FF15273F"
                          Data="F1 M 399,86L 403,93L 409,81L 403,90L 399,86 Z"
                          Height="12" Width="10" UseLayoutRounding="True"/>
                </Border>
            </Border>    
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="FlowDirection" Value="RightToLeft">
                <Setter TargetName="Root" Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleY="1" ScaleX="-1"/>
                    </Setter.Value>
                </Setter>
            </Trigger>        
        </ControlTemplate.Triggers>         
    </ControlTemplate> 

    <Style x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=SelectedTextStyleInTextSplitter}" TargetType="{x:Type ContentControl}">
        <Setter Property="Foreground" Value="#FF000000"/>
    </Style>

    <Style x:Key="{dxbt:BarCheckItemThemeKey ResourceKey=NormalTextStyleInTextSplitter}" TargetType="{x:Type ContentControl}">
        <Setter Property="Foreground" Value="#FF000000"/>
    </Style>

</ResourceDictionary>