<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduler" xmlns:dxscht="http://schemas.devexpress.com/winfx/2008/xaml/scheduler/themekeys" xmlns:dxschcore="clr-namespace:DevExpress.XtraScheduler;assembly=DevExpress.XtraScheduler.v15.2.Core" xmlns:dxschd="http://schemas.devexpress.com/winfx/2008/xaml/scheduler/internal" xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">

    <DataTemplate x:Key="{dxscht:SchedulerViewThemeKey ResourceKey=HorizontalAppointmentSameDayContentTemplate}">
        <dxschd:HorizontalAppointmentContentPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <dxschd:HorizontalAppointmentStartClockControl VerticalAlignment="Center" ViewInfo="{Binding}" />
            <dxschd:HorizontalAppointmentEndClockControl VerticalAlignment="Center" ViewInfo="{Binding}" />
            <dxschd:AppointmentImagesControl ViewInfo="{Binding}" Orientation="Horizontal" />
            <TextBlock Text="{Binding Subject}" 
                       Foreground="{DynamicResource {dxscht:SchedulerViewThemeKey ResourceKey=AppointmentForegroundColor}}"
                       TextWrapping="Wrap"
                       TextAlignment="Left" 
                       VerticalAlignment="Stretch"
                       HorizontalAlignment="Stretch" 
                       Margin="1,0,2,0">
                <!--BEGIN WPF-->
                <TextBlock.TextTrimming>
                    <TextTrimming>
                        CharacterEllipsis
                    </TextTrimming>
                </TextBlock.TextTrimming>
                <!--END WPF-->
            </TextBlock>
            <TextBlock Text="{Binding Location}" 
                       Foreground="{DynamicResource {dxscht:SchedulerViewThemeKey ResourceKey=AppointmentForegroundColor}}"
                       TextWrapping="Wrap"
                       TextAlignment="Left" 
                       VerticalAlignment="Stretch"
                       HorizontalAlignment="Stretch" 
                       >
                <!--BEGIN WPF-->
                <TextBlock.TextTrimming>
                    <TextTrimming>
                        CharacterEllipsis
                    </TextTrimming>
                </TextBlock.TextTrimming>
                <!--END WPF-->
            </TextBlock>
        </dxschd:HorizontalAppointmentContentPanel>
    </DataTemplate>
    
</ResourceDictionary>