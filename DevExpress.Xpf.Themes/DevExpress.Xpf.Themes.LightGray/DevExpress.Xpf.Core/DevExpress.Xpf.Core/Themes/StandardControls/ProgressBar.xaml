<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
    mc:Ignorable="d">

    <Style x:Key="{dxt:DefaultStyleThemeKey Type=ProgressBar}" TargetType="{x:Type ProgressBar}">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground">
            <Setter.Value>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FFC2D9F8" Offset="0" />
                    <GradientStop Color="#FFB1CDF5" Offset="1" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Background">
            <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFFEFEFE" Offset="1" />
                        <GradientStop Color="#FFEAEAEA" />
                    </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <ControlTemplate.Resources>
                        <Storyboard RepeatBehavior="Forever" x:Key="storyboardIndeterminate">
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(Brush.Transform).(TransformGroup.Children)[0].X" Storyboard.TargetName="border_IsIndeterminate">
                                <SplineDoubleKeyFrame KeyTime="0:0:0.5" Value="20" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid x:Name="Background" SnapsToDevicePixels="true">
                        <Border x:Name="PART_Track" CornerRadius="2">
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FFA9B8CA" Offset="0" />
                                    <GradientStop Color="#FFA2B9D9" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border x:Name="border_track" Margin="1" CornerRadius="1" Background="{TemplateBinding Background}"/>
                        </Border>
                        <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left" Margin="1">
                            <Grid x:Name="Foreground">
                                <Border x:Name="border" CornerRadius="1" Background="{TemplateBinding Foreground}"/>
                                <Border x:Name="border_IsIndeterminate" CornerRadius="1" Opacity="0">
                                    <Border.Background>
                                        <LinearGradientBrush EndPoint="0,1" MappingMode="Absolute" StartPoint="20,1" SpreadMethod="Repeat">
                                            <LinearGradientBrush.Transform>
                                                <TransformGroup>
                                                    <TranslateTransform X="0" />
                                                    <SkewTransform AngleX="45" />
                                                </TransformGroup>
                                            </LinearGradientBrush.Transform>
                                            <GradientStop Color="#FFA2B9D9" />
                                            <GradientStop Color="#FFA2B9D9" Offset="0.5" />
                                            <GradientStop Color="#FFE3EDFB" Offset="0.5" />
                                            <GradientStop Color="#FFE3EDFB" Offset="1" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </Decorator>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Border.BorderThickness" Value="0">
                            <Setter Property="Margin" TargetName="border_track" Value="0" />
                            <Setter Property="Margin" TargetName="PART_Indicator" Value="0" />
                            <Setter Property="CornerRadius" TargetName="PART_Track" Value="0" />
                            <Setter Property="CornerRadius" TargetName="border" Value="0" />
                            <Setter Property="CornerRadius" TargetName="border_IsIndeterminate" Value="0" />
                            <Setter Property="CornerRadius" TargetName="border_track" Value="0" />
                        </Trigger>
                        <Trigger Property="Panel.Background" Value="{x:Null}">
                            <Setter Property="Visibility" TargetName="PART_Track" Value="Hidden" />
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter Property="LayoutTransform" TargetName="Background">
                                <Setter.Value>
                                    <RotateTransform CenterX="25" CenterY="25" Angle="-90" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsIndeterminate" Value="true">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource storyboardIndeterminate}" />
                            </Trigger.EnterActions>
                            <Setter Property="Opacity" TargetName="border_IsIndeterminate" Value="1" />
                            <Setter Property="Opacity" TargetName="border" Value="0" />
                        </Trigger>
                        <Trigger Property="IsIndeterminate" Value="false">
                            <Setter Property="Opacity" TargetName="border" Value="1" />
                            <Setter Property="Opacity" TargetName="border_IsIndeterminate" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>