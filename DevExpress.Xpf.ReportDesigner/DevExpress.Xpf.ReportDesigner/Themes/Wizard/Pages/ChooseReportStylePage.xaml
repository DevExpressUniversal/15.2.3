<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dan ="clr-namespace:DevExpress.Xpf.DataAccess.Native;assembly=DevExpress.Xpf.DataAccess.v15.2"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:local="clr-namespace:DevExpress.Xpf.Reports.UserDesigner.ReportWizard.Pages">
    <DataTemplate x:Key="{x:Type local:ChooseReportStylePage}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <dan:WizardTitle Title="The report style specifies the appearance of your report."/>
            <Grid Grid.Row="1" Margin="50,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.Resources>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="LightGray" Offset="0.6" />
                                        <GradientStop Color="Transparent" Offset="0.9" />
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="White" Offset="0.7" />
                                        <GradientStop Color="Transparent" Offset="1" />
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Grid.Resources>
                    <Border  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" CornerRadius="3,3,3,3" BorderThickness="1,1,1,1">
                        <Grid>
                            <Border Margin="26,0,26,0" BorderThickness="1,0,1,0" Background="Transparent" />
                            <Border Margin="0,26,0,26" BorderThickness="0,1,0,1" Background="Transparent"/>
                            <Border Margin="26,26,26,26" BorderBrush="Transparent" Background="Transparent">
                                <StackPanel>
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{Binding FontFamily}" />
                                            <Setter Property="Foreground">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="{Binding FontColor}" />
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="FontSize" Value="{Binding FontSize}" />
                                            <Setter Property="FontStyle" Value="{Binding FontStyle}" />
                                            <Setter Property="FontWeight" Value="{Binding FontWeight}" />
                                        </Style>
                                    </StackPanel.Resources>
                                    <TextBlock Margin="24" DataContext="{Binding SelectedReportStyle.Title }" Text="Title" />
                                    <TextBlock Margin="24,0,0,16" DataContext="{Binding SelectedReportStyle.Caption }" Text="Caption" />
                                    <TextBlock Margin="24,0,0,0" DataContext="{Binding SelectedReportStyle.Data }" Text="Data" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
                <dxe:ListBoxEdit ShowBorder="False" Margin="18,12,0,0" Grid.Column="1" ItemsSource="{Binding ReportStyles}" EditValue="{Binding SelectedReportStyle, UpdateSourceTrigger=PropertyChanged}">
                    <dxe:ListBoxEdit.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Margin="0,0,0,6"/>
                        </DataTemplate>
                    </dxe:ListBoxEdit.ItemTemplate>
                    <dxe:ListBoxEdit.StyleSettings>
                        <dxe:RadioListBoxEditStyleSettings />
                    </dxe:ListBoxEdit.StyleSettings>
                </dxe:ListBoxEdit>
            </Grid>
        </Grid>
    </DataTemplate>
</ResourceDictionary>