<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="clr-namespace:DevExpress.Xpf.DataAccess.DataSourceWizard"
            xmlns:localeditors="clr-namespace:DevExpress.Xpf.DataAccess.Editors"
            xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
            xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
            xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
            xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid">

    <Style TargetType="{x:Type localeditors:DataSourceWizardExpressionUITypeEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="localeditors:DataSourceWizardExpressionUITypeEditor">
                    <Grid>
                        <localeditors:ExpressionEditor x:Name="expressionEditor" 
                                                       EditValue="{Binding EditValue, RelativeSource={RelativeSource TemplatedParent}}"
                                                       EditorLogicFunc="{Binding EditorLogicFunc, RelativeSource={RelativeSource TemplatedParent}}"
                                                       />

                        <dx:DialogFooter>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <dx:DialogButton DialogResult="OK" Command="{Binding SaveCommand, ElementName=expressionEditor}" />
                                <dx:DialogButton DialogResult="Cancel" />
                            </StackPanel>
                        </dx:DialogFooter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>