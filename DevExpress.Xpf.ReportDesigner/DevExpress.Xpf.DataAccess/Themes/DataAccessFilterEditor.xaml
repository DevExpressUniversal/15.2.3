<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="clr-namespace:DevExpress.Xpf.DataAccess.DataSourceWizard"
            xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
            xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
            xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
            xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid">

    <Style TargetType="{x:Type local:DataAccessFilterEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DataAccessFilterEditor}">
                    <Grid>
                        <dxe:FilterControl
                                    x:Name="filterControl"
                                    FilterCriteria="{Binding WizardFilterCriteria, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    ShowOperandTypeIcon="{Binding ShowOperandTypeIcon, RelativeSource={RelativeSource TemplatedParent}}"
                                    ShowGroupCommandsIcon="{Binding ShowGroupCommandsIcon, RelativeSource={RelativeSource TemplatedParent}}"
                        >
                            <dxmvvm:Interaction.Behaviors>
                                <dxe:FilterControlColumnsBehavior AdditionalProperties="{Binding AdditionalProperties, RelativeSource={RelativeSource TemplatedParent}}" ObjectType="{Binding ObjectType, RelativeSource={RelativeSource TemplatedParent}}" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:FilterControl>
                        <dx:DialogFooter>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <dx:DialogButton DialogResult="OK" Command="{Binding ApplyFilterCommand, ElementName=filterControl}"/>
                                <dx:DialogButton DialogResult="Cancel" />
                            </StackPanel>
                        </dx:DialogFooter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>