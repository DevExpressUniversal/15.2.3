<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="clr-namespace:DevExpress.Xpf.DataAccess.DataSourceWizard"
            xmlns:localnative="clr-namespace:DevExpress.Xpf.DataAccess.Native"
            xmlns:dxdae="clr-namespace:DevExpress.Xpf.DataAccess.Editors"
            xmlns:localwaitform="clr-namespace:DevExpress.Xpf.DataAccess.WaitForm"
            xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
            xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
            xmlns:dxco="http://schemas.devexpress.com/winfx/2008/xaml/controls"
            xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
            xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation"
            xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
            xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
            xmlns:dxpg="http://schemas.devexpress.com/winfx/2008/xaml/propertygrid"
            xmlns:localization="clr-namespace:DevExpress.Xpf.DataAccess.Localization"
            xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/DevExpress.Xpf.DataAccess.v15.2;component/Themes/Wizard/Pages.xaml" />
    </ResourceDictionary.MergedDictionaries>
    
    <Style TargetType="local:DataSourceWizardControl">
        <Setter Property="MessageBoxServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DXMessageBoxService />
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SplashScreenServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DXSplashScreenService SplashScreenStartupLocation="CenterOwner">
                            <dx:DXSplashScreenService.ViewTemplate>
                                <DataTemplate>
                                    <dx:WaitIndicator DeferedVisibility="True" />
                                </DataTemplate>
                            </dx:DXSplashScreenService.ViewTemplate>
                        </dx:DXSplashScreenService>
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="OpenFileDialogServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dxmvvm:OpenFileDialogService />
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="QueryBuilderDialogServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DialogService>
                            <dx:DialogService.DialogStyle>
                                <Style TargetType="dx:DXDialogWindow">
                                    <Setter Property="UseLayoutRounding" Value="True" />
                                </Style>
                            </dx:DialogService.DialogStyle>
                            <dx:DialogService.ViewTemplate>
                                <DataTemplate>
                                    <local:QueryBuilder EditValue="{Binding}" />
                                </DataTemplate>
                            </dx:DialogService.ViewTemplate>
                        </dx:DialogService>
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="PreviewDialogServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DialogService>
                            <dx:DialogService.DialogStyle>
                                <Style TargetType="dx:DXDialogWindow">
                                    <Setter Property="UseLayoutRounding" Value="True" />
                                </Style>
                            </dx:DialogService.DialogStyle>
                            <dx:DialogService.ViewTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <dxg:GridControl ItemsSource="{Binding}" AutoGenerateColumns="AddNew">
                                            <dxg:GridControl.View>
                                                <dxg:TableView
                                                            BestFitMaxRowCount="1000"
                                                            AllowEditing="False"
                                                            AllowFilterEditor="False"
                                                            AllowColumnFiltering="False"
                                                            AutoWidth="False"
                                                            ShowIndicator="False"
                                                            ShowGroupPanel="False"
                                                            ShowFilterPanelMode="Never"
                                                            IsColumnMenuEnabled="False"
                                                />
                                            </dxg:GridControl.View>
                                        </dxg:GridControl>
                                        <dx:DialogFooter>
                                            <dx:DialogButton Content="Close" DialogResult="OK" HorizontalAlignment="Right" />
                                        </dx:DialogFooter>
                                    </Grid>
                                </DataTemplate>
                            </dx:DialogService.ViewTemplate>
                        </dx:DialogService>
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="PasswordDialogServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DialogService>
                            <dx:DialogService.DialogStyle>
                                <Style TargetType="dx:DXDialogWindow">
                                    <Setter Property="UseLayoutRounding" Value="True" />
                                    <Setter Property="Width" Value="400" />
                                    <Setter Property="SizeToContent" Value="Height" />
                                </Style>
                            </dx:DialogService.DialogStyle>
                            <dx:DialogService.ViewTemplate>
                                <DataTemplate>
                                    <localnative:PasswordRequestDialog EditValue="{Binding}" />
                                </DataTemplate>
                            </dx:DialogService.ViewTemplate>
                        </dx:DialogService>
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ChooseEFStoredProceduresDialogServiceTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ContentControl>
                        <dx:DialogService>
                            <dx:DialogService.DialogStyle>
                                <Style TargetType="{x:Type dx:DXDialogWindow}">
                                    <Setter Property="UseLayoutRounding" Value="True" />
                                    <Setter Property="Width" Value="350" />
                                    <Setter Property="Height" Value="400" />
                                    <Setter Property="ResizeMode" Value="CanResize" />
                                </Style>
                            </dx:DialogService.DialogStyle>
                            <dx:DialogService.ViewTemplate>
                                <DataTemplate>
                                    <dxdae:ChooseEFStoredProceduresEditor EditValue="{Binding EditValue}" ItemsSource="{Binding ItemsSource}" />
                                </DataTemplate>
                            </dx:DialogService.ViewTemplate>
                        </dx:DialogService>
                    </ContentControl>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DataSourceWizardControl">
                    <Grid>
                        <dxco:Wizard x:Name="Wizard" ViewTemplateSelector="{Binding PageTemplateSelector, RelativeSource={RelativeSource TemplatedParent}}">
                            <dxco:Wizard.FooterTemplate>
                                <DataTemplate>
                                    <dx:DialogFooter>
                                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                            <dxco:WizardButton Content="{localization:DataAccessUIStringId StringId=Button_Previous}" dxco:Wizard.IsButtonBack="True" />
                                            <dxco:WizardButton Content="{localization:DataAccessUIStringId StringId=Button_Next}" dxco:Wizard.IsButtonNext="True" />
                                            <dxco:WizardButton Content="{localization:DataAccessUIStringId StringId=Button_Finish}" dxco:Wizard.IsButtonFinish="True" />
                                        </StackPanel>
                                    </dx:DialogFooter>
                                </DataTemplate>
                            </dxco:Wizard.FooterTemplate>
                        </dxco:Wizard>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>