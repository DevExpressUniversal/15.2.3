<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DevExpress.Xpf.DataAccess.DataSourceWizard"
                     xmlns:localnative="clr-namespace:DevExpress.Xpf.DataAccess.Native"
                    xmlns:localization="clr-namespace:DevExpress.Xpf.DataAccess.Localization"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">
    <DataTemplate x:Key="{x:Type local:ChooseEFDataMemberPage}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <localnative:WizardTitle Title="{localization:DataAccessUIStringId StringId=WizardPageChooseEFDataMember}" />
            <dxe:ListBoxEdit Grid.Row="1" Margin="40,10" ItemsSource="{Binding DataMembers}"  EditValue="{Binding SelectedDataMember, UpdateSourceTrigger=PropertyChanged}">
                <dxe:ListBoxEdit.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding DataMemberType, Converter={local:EFDataMemberTypeToImageConverter}}" HorizontalAlignment="Left"/>
                            <TextBlock Text="{Binding Name}" Margin="3,0"  />
                        </StackPanel>
                    </DataTemplate>
                </dxe:ListBoxEdit.ItemTemplate>
                <dxe:ListBoxEdit.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </dxe:ListBoxEdit.ItemsPanel>
            </dxe:ListBoxEdit>
        </Grid>
    </DataTemplate>
</ResourceDictionary>