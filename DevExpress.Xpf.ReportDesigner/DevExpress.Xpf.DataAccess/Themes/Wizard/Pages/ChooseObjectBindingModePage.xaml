<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
            xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
            xmlns:editors="clr-namespace:DevExpress.Xpf.DataAccess.Editors"
            xmlns:local="clr-namespace:DevExpress.Xpf.DataAccess.DataSourceWizard"
            xmlns:localnative="clr-namespace:DevExpress.Xpf.DataAccess.Native"
            xmlns:localization="clr-namespace:DevExpress.Xpf.DataAccess.Localization">

    <DataTemplate x:Key="{x:Type local:ChooseObjectBindingModePage}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <localnative:WizardTitle Title="{localization:DataAccessUIStringId StringId=WizardPageChooseObjectBindingMode}" />
            <dxe:ListBoxEdit Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalContentAlignment="Top" Padding="5"
                        ItemsSource="{Binding Options}"
                        ValueMember="Value"
                        EditValue="{Binding SchemaOnly, UpdateSourceTrigger=PropertyChanged}"
                        StyleSettings="{editors:RadioListBoxEditStyleSettingsEx}">
                <dxe:ListBoxEdit.ItemContainerStyle>
                    <Style TargetType="dxe:ListBoxEditItem">
                        <Setter Property="Margin" Value="5" />
                    </Style>
                </dxe:ListBoxEdit.ItemContainerStyle>
                <dxe:ListBoxEdit.ItemTemplate>
                    <DataTemplate>
                        <StackPanel VerticalAlignment="Bottom" Margin="0,-5,0,0">
                            <TextBlock Text="{Binding Text}" FontWeight="Bold" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </dxe:ListBoxEdit.ItemTemplate>
            </dxe:ListBoxEdit>
        </Grid>
    </DataTemplate>
</ResourceDictionary>