<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:DevExpress.Design.UI"
    xmlns:eufUI="clr-namespace:DevExpress.Design.Filtering.UI"
    xmlns:eufEditorTypes="clr-namespace:DevExpress.Design.Filtering"
    >
    <DataTemplate x:Key="MetricEditorTypeRangeTemplate">
        <TextBlock
                  Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=Value}}"
                  Text="Range" />
    </DataTemplate>
    <DataTemplate x:Key="MetricEditorTypeLookupTemplate">
        <TextBlock
                  Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=Value}}"
                  Text="Lookup" />
    </DataTemplate>
    <DataTemplate x:Key="MetricEditorTypeBooleanChoiceTemplate">
        <TextBlock
                  Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=Value}}"
                  Text="Boolean Choice" />
    </DataTemplate>
    <DataTemplate x:Key="MetricEditorTypeEnumChoiceTemplate">
        <TextBlock
                  Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=Value}}"
                  Text="Enum Choice" />
    </DataTemplate>
</ResourceDictionary>