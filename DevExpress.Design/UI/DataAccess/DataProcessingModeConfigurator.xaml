<UserControl x:Class="DevExpress.Design.DataAccess.UI.DataProcessingModeConfigurator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:DevExpress.Design.UI"
	     Background="Transparent"
             >
    <UserControl.Resources>
        <Style TargetType="Hyperlink">
            <Setter Property="Foreground" Value="{StaticResource ResourceKey={ui:ThemeColorsKey Key=ColorOrange}}"/>
        </Style>
    </UserControl.Resources>
    <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=ListCaption}}"/>
        <ListView x:Name="bindingListSelector" Grid.Row="1" Style="{DynamicResource ResourceKey={ui:StandardControlsThemeKey Key=ListBox}}"
                    ItemsSource="{Binding ProcessingModes}" 
                    SelectedItem="{Binding SelectedProcessingMode}" 
                    DisplayMemberPath="Name"
		    ui:ListViewDoubleClickBehavior.Command="{Binding ItemDoubleClickCommand}"
                  />
        <Border Grid.Column="2" Grid.Row="1" 
                BorderThickness="1" BorderBrush="{StaticResource ResourceKey={ui:ThemeColorsKey Key=ColorSilverLight}}" >
            <TextBlock ui:HyperlinkInlineExpression.InlineExpression="{Binding SelectedProcessingMode.Description}" 
                       Style="{DynamicResource ResourceKey={ui:ControlsThemeKey Key=Description}}"
                       />
        </Border>
    </Grid>
</UserControl>