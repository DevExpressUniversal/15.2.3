<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.ChangeChartItemTitleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="350">
  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=edtTitle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="315"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding LabelText}" VerticalAlignment="Center" Margin="0,0,6,0"/>
        <dxe:TextEdit x:Name="edtTitle" Grid.Column="1" EditValue="{Binding Title}"/>
    </Grid>
</UserControl>
