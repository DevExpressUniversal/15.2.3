<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.ConfirmPasswordControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
      xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
      mc:Ignorable="d" Width="320"
      d:DesignHeight="300" d:DesignWidth="300">

  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=edtPassword}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ConfirmPasswordFormPassword, Converter={StaticResource stringIdConverter}}" Margin="0,0,0,3"/>
        <dxe:PasswordBoxEdit x:Name="edtPassword" Grid.Row="1" EditValue="{Binding Password}" />
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ConfirmPasswordFormDescription, Converter={StaticResource stringIdConverter}}" Grid.Row="2" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
