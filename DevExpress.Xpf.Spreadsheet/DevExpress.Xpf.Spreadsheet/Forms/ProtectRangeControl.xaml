<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.ProtectRangeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d" Width="425"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
        <dxc:BooleanToVisibilityConverter x:Key="boolToVisibilityConverter" />
        <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=edtTitle}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ProtectedRangeFormName, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center" Margin="0,6,0,6" />
        <dxe:TextEdit x:Name="edtTitle" EditValue="{Binding Title}" Margin="0,0,0,6" Grid.Row="1" />
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ProtectedRangeFormReference, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center" Margin="0,6,0,6" Grid.Row="2" />
        <dxe:TextEdit x:Name="edtReference" EditValue="{Binding Reference}" Margin="0,0,0,6" Grid.Row="3" />
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ProtectedRangeFormPassword, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center"  Visibility="{Binding AllowToSetNewPassword, Converter={StaticResource ResourceKey=boolToVisibilityConverter}}" Margin="0,6,0,6" Grid.Row="4" />
        <dxe:PasswordBoxEdit x:Name="edtPassword" EditValue="{Binding Password}" Visibility="{Binding AllowToSetNewPassword, Converter={StaticResource ResourceKey=boolToVisibilityConverter}}" Margin="0,0,0,6" Grid.Row="5" />
        <Grid Grid.Row="6" Visibility="{Binding HasPassword, Converter={StaticResource ResourceKey=boolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnSetPassword" Click="btnSetPassword_Click" Margin="0,0,0,6" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ProtectedRangeFormSetPassword, Converter={StaticResource stringIdConverter}}" />
        </Grid>
        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnPermissions" Click="btnPermissions_Click" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_ProtectedRangeFormPermissions, Converter={StaticResource stringIdConverter}}" />
        </Grid>
    </Grid>
</UserControl>
