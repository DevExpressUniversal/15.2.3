<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.UnhideSheetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d" Height="150" Width="300"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=listbox}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_UnhideSheetFormUnhideSheet, Converter={StaticResource stringIdConverter}}" Margin="0,0,0,8" HorizontalAlignment="Left"/>
        <dxe:ListBoxEdit x:Name="listbox" Grid.Row="1" ItemsSource="{Binding HiddenSheetNames}" SelectionMode="Single" SelectedItem="{Binding SheetName}" 
                         EditValuePostMode="Immediate" MouseDoubleClick="ListBoxEditMouseDoubleClick"/>
        <!--EditValue="{Binding SelectedSheetNames}"-->
    </Grid>
</UserControl>
