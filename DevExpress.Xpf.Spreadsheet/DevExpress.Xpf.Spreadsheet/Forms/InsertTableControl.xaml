<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.InsertTableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d" Width="425"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
    <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=edtRange}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_InsertTableFormDataLocation, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center" Margin="0,0,0,6" Grid.Row="0"/>
        <dxe:TextEdit x:Name="edtRange" EditValue="{Binding Reference}" Grid.Row="1"/>
        <dxe:CheckEdit Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_InsertTableFormTableHasHeaders, Converter={StaticResource stringIdConverter}}" EditValue="{Binding HasHeaders}" VerticalAlignment="Center" Margin="0,6,0,0" Grid.Row="2"/>
    </Grid>
</UserControl>
