<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.CreateDefinedNamesFromSelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d" Width="425"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <Grid Margin="10,10,12,0" FocusManager.FocusedElement="{Binding ElementName=chkUseTopRow}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_CreateDefinedNamesFromSelectionFormCreateNameValues, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center" Margin="0,0,0,12" />
        <dxe:CheckEdit x:Name="chkUseTopRow" Grid.Row="1" Margin="0,6,0,0" IsChecked="{Binding UseTopRow}" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_CreateDefinedNamesFromSelectionFormTopRow, Converter={StaticResource stringIdConverter}}" />
        <dxe:CheckEdit Grid.Row="2" Margin="0,6,0,0" IsChecked="{Binding UseLeftColumn}" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_CreateDefinedNamesFromSelectionFormLeftColumn, Converter={StaticResource stringIdConverter}}" />
        <dxe:CheckEdit Grid.Row="3" Margin="0,6,0,0" IsChecked="{Binding UseBottomRow}" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_CreateDefinedNamesFromSelectionFormBottomRow, Converter={StaticResource stringIdConverter}}" />
        <dxe:CheckEdit Grid.Row="4" Margin="0,6,0,0" IsChecked="{Binding UseRightColumn}" Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_CreateDefinedNamesFromSelectionFormRightColumn, Converter={StaticResource stringIdConverter}}" />
    </Grid>
</UserControl>
