<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.FormatNumberTypeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormNumberType, Converter={StaticResource stringIdConverter}}"/>
        <dxe:ListBoxEdit Grid.Row="1"
                         Margin="0,3,0,0"
                         ItemsSource="{Binding TypeCaptions}"
                         SelectedIndex="{Binding SelectedTypeIndex, Mode=TwoWay}"
                         SelectedItem="{Binding SelectedType, Mode=OneWay}"/>
        <TextBlock Grid.Row="2"
                   Margin="0,3,0,0"
                   Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormNumberLocation, Converter={StaticResource stringIdConverter}}"/>
        <dxe:ComboBoxEdit Grid.Row="3"
                          Margin="0,3,0,0"
                          EditValue="English (United States)"
                          IsEnabled="False"/>
    </Grid>
</UserControl>
    
