<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.FormatNumberCustomControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormNumberType, Converter={StaticResource stringIdConverter}}"/>
        <dxe:TextEdit Grid.Row="1"
                      Margin="0,3,0,0"
                      EditValue="{Binding CustomFormatCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <dxe:ListBoxEdit Grid.Row="2" Visibility="Hidden"/>
    </Grid>
</UserControl>
