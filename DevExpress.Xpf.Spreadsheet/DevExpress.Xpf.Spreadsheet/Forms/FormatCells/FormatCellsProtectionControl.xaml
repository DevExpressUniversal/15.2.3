<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.FormatCellsProtectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxspsi="clr-namespace:DevExpress.Xpf.Spreadsheet.Internal"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <dxspsi:CheckStateToBoolConverter x:Key="CheckStateToBoolConverter"/>
        <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <dxe:CheckEdit Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormProtectionLocked, Converter={StaticResource stringIdConverter}}"
                       EditValue="{Binding Locked, Mode=TwoWay, Converter={StaticResource CheckStateToBoolConverter}}"
                       IsThreeState="{Binding LockedIndeterminate}"/>
        <dxe:CheckEdit Grid.Row="1" 
                       Margin="0,5,0,0"
                       Content="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormProtectionHidden, Converter={StaticResource stringIdConverter}}"
                       EditValue="{Binding Hidden, Mode=TwoWay, Converter={StaticResource CheckStateToBoolConverter}}" 
                       IsThreeState="{Binding HiddenIndeterminate}"/>
        <TextBlock Grid.Row="2"
                   Margin="0,5,0,0"
                   HorizontalAlignment="Left"
                   TextAlignment="Left"
                   TextWrapping="Wrap"
                   Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_FormatCellsFormProtectionDescription, Converter={StaticResource stringIdConverter}}"/>
    </Grid>
</UserControl>