<UserControl x:Class="DevExpress.Xpf.Spreadsheet.Forms.PasteSpecialFormControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxspsi="clr-namespace:DevExpress.Xpf.Spreadsheet.Internal"
             xmlns:dxsps="clr-namespace:DevExpress.Xpf.Spreadsheet" 
             mc:Ignorable="d" Height="180" Width="410"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <dxsps:SpreadsheetControlStringIdConverter x:Key="stringIdConverter" />
  </UserControl.Resources>
  <!--<UserControl.Resources>
        <dxspsi:BoolToIntConverter x:Key="BoolToIntConverter"/>
    </UserControl.Resources>-->
    <Grid Margin="12,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!--<RowDefinition Height="Auto"/>-->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource stringIdConverter}, ConverterParameter=Caption_PasteSpecialFormPasteAs, Converter={StaticResource stringIdConverter}}" VerticalAlignment="Center" Grid.Row="0" Margin="0,0,0,6"/>
        <dxe:ListBoxEdit Grid.Row="1" ItemsSource="{Binding AvailableCommandTypes}" SelectedItem="{Binding SelectedCommanndType, Mode=TwoWay}"/>
    </Grid>
</UserControl>
