<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevExpress.Xpf.Map">

    <local:BoolToVisibilityConverter x:Key="VisibilityConverter"/>

    <Style TargetType="local:MapScalePanelLayoutControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MapScalePanelLayoutControl">
                    <local:MapScalePanel
                            x:Name="PART_ScalePanel"
                            ShowKilometersScale="{Binding Path=Options.ShowKilometersScale}"
                            ShowMilesScale="{Binding Path=Options.ShowMilesScale}"
                            KilometersScale="{Binding Path=KilometersScale}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="local:MapScalePanel">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MapScalePanel">
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" MinHeight="29" Margin="2" HorizontalAlignment="Right">
                            <Border Background="Black" Opacity="0.6" CornerRadius="3" 
                                Visibility="{Binding ShowKilometersScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}"/>
                            <StackPanel Orientation="Vertical" Margin="16,6" HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Visibility="{Binding ShowKilometersScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}">
                                <TextBlock TextWrapping="Wrap" Text="{Binding KilometersScaleText, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,-2"/>
                                <Border BorderBrush="White" BorderThickness="1,0,1,2" Height="6" Width="{Binding KilometersScaleBarWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <Grid Grid.Row="2" MinHeight="29" Margin="2" HorizontalAlignment="Right">
                            <Border Background="Black" Opacity="0.6" CornerRadius="3" 
                                Visibility="{Binding ShowMilesScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}"/>
                            <StackPanel Orientation="Vertical" Margin="16,6" HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Visibility="{Binding ShowMilesScale, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource VisibilityConverter}}">
                                <TextBlock TextWrapping="Wrap" Text="{Binding MilesScaleText, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-3,0,-2"/>
                                <Border BorderBrush="White" BorderThickness="1,0,1,2" Height="6" Width="{Binding MilesScaleBarWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
