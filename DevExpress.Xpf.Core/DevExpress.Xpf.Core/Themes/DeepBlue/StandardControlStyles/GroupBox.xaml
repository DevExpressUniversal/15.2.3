<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:dx="clr-namespace:DevExpress.Xpf.Core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dxtEx="clr-namespace:DevExpress.Xpf.Utils.Themes"
    xmlns:dxt="clr-namespace:DevExpress.Xpf.Utils.Themes"
                    >
    <ResourceDictionary.MergedDictionaries>
		<dxtEx:ResourceDictionaryEx Source="{dx:UtilsResource Themes/DeepBlue/StandardControlStyles/Brushes.xaml}" />
	</ResourceDictionary.MergedDictionaries>
	<Thickness x:Key="elementMargin" Bottom="20" />
	<Thickness x:Key="groupMargin" Bottom="10" />
	<Thickness x:Key="controlMargin" Bottom="5" />
    <ControlTemplate x:Key="{dxt:GroupBoxThemeKey ResourceKey=ControlTemplate}" TargetType="{x:Type GroupBox}">
        <Grid SnapsToDevicePixels="true">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border CornerRadius="3,3,3,3" Padding="1,1,1,1" x:Name="panel_bg" Margin="0,0,0,0" Background="#FF5A81BD" Opacity="0.35" VerticalAlignment="Stretch" Height="Auto" Grid.Row="1" Grid.RowSpan="2">
                <Border CornerRadius="2,2,2,2" Height="Auto" Background="#FFCBE2FF" Width="Auto" />
            </Border>
            <Border Grid.RowSpan="2" Padding="1,1,1,1" HorizontalAlignment="Center" CornerRadius="9,9,9,9" x:Name="header_container" Background="#FF7897D0">
                <Grid Width="Auto" Height="Auto">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.5*" />
                        <RowDefinition Height="0.5*" />
                    </Grid.RowDefinitions>
                    <Border Grid.RowSpan="2" CornerRadius="8,8,8,8">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FF8AB3E2" Offset="0" />
                                <GradientStop Color="#FFB2CDF8" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <Border Grid.RowSpan="1" CornerRadius="8,8,3,3" Margin="1,0,1,0">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,-0.002" StartPoint="0.5,1.002">
                                <GradientStop Color="#FFB9D4F6" Offset="1" />
                                <GradientStop Color="#FFAACAF8" Offset="0" />
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <Border x:Name="header" Background="{x:Null}" CornerRadius="2,2,2,2" Padding="16,1,16,1" Width="Auto" Height="Auto" Grid.RowSpan="2">
                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ContentSource="Header" RecognizesAccessKey="True" TextElement.Foreground="Black" Width="Auto" Height="Auto" VerticalAlignment="Center" HorizontalAlignment="Stretch" />
                    </Border>
                </Grid>
            </Border>
            <Border Grid.RowSpan="1" Grid.Row="2" Padding="12" Name="content_border">
                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Width="Auto" Height="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasHeader" Value="false">
                <Setter Property="Visibility" Value="Collapsed" TargetName="header_container" />
                <Setter Property="Padding" Value="12" TargetName="content_border" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>