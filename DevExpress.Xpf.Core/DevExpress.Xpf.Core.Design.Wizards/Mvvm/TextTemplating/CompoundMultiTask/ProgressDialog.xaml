<Window x:Class="DevExpress.Xpf.Core.Design.Wizards.Mvvm.TextTemplating.ProgressDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DevExpress.Xpf.Core.Design.Wizards.Mvvm.TextTemplating"
             Height="Auto" Width="Auto" WindowStyle="None" ResizeMode="NoResize"
             AllowsTransparency ="True" WindowStartupLocation="CenterOwner"  SizeToContent="WidthAndHeight"
             Background="{x:Null}"
             >    
    <Window.DataContext>
        <local:ProgressDialogViewModel/>
    </Window.DataContext>
    <Grid Margin="32">
        <Border BorderBrush="#26000000" BorderThickness="1" Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="32" Direction="-90" Opacity="0.35" ShadowDepth="8" />
            </Border.Effect>
        </Border>
        <DockPanel Margin="15">
            <TextBlock DockPanel.Dock="Top" TextAlignment="Left" Width="400" Margin="0,0,0,10" Text="{Binding Description}" TextWrapping="Wrap"/>
            <ProgressBar Value="{Binding Progress}" Minimum="0" Maximum="1" Height="30"/>
        </DockPanel>
    </Grid>
</Window>
