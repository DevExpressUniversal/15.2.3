<UserControl x:Class="DevExpress.Xpf.Core.Design.SmartTags.BindingEditorControlElementSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:utils="clr-namespace:DevExpress.Xpf.Core.Design.CoreUtils"
        xmlns:local="clr-namespace:DevExpress.Xpf.Core.Design.SmartTags"
        mc:Ignorable="d" 
        d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:DesignTimeTreeViewResources/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="ListBoxItem" />
            <Style TargetType="ListBox" />
            <Style TargetType="GroupItem" />
            <utils:ItemToEnumerableConverter x:Key="ItemToEnumerableConverter" />
            <dx:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <Style x:Key="ElementTreeViewItemStyle" BasedOn="{StaticResource DesignTimeTreeViewItemStyle}" TargetType="local:DesignTimeTreeViewItem">
                <Setter Property="Visibility" Value="{Binding Path=IsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                <Setter Property="ShowExpander" Value="{Binding Path=HasVisibleChildren}" />
                <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Setter Property="CanBeSelected" Value="{Binding Path=CanBeSelected}" />
                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <dxe:TextEdit x:Name="searchBox" NullText="Type to search..." EditValue="{Binding Path=FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,3" />
        <local:DesignTimeTreeView Grid.Row="1" ItemsSource="{Binding Path=RootTreeNodeViewModel, Converter={StaticResource ItemToEnumerableConverter}}" ItemContainerStyle="{StaticResource ElementTreeViewItemStyle}">
            <local:DesignTimeTreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                    <StackPanel Orientation="Horizontal">
                        <local:WrapHighlightTextBlock Opacity="0.8" Text="{Binding Path=DisplayText}" SearchText="{Binding Path=SearchText}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </local:DesignTimeTreeView.ItemTemplate>
        </local:DesignTimeTreeView>
    </Grid>
</UserControl>
