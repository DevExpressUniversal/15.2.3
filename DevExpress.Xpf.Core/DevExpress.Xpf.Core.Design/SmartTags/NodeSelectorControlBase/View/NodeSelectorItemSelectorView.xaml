<UserControl x:Class="DevExpress.Xpf.Core.Design.SmartTags.NodeSelectorItemSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:coreutils="clr-namespace:DevExpress.Xpf.Core.Design.CoreUtils"
        xmlns:local="clr-namespace:DevExpress.Xpf.Core.Design.SmartTags"
        mc:Ignorable="d" 
        d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <dx:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style x:Key="ItemTreeViewItemStyle" TargetType="local:DesignTimeTreeViewItem">
            <Setter Property="Visibility" Value="{Binding Path=IsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Setter Property="ShowExpander" Value="{Binding Path=HasVisibleChildren}" />
            <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="CanBeSelected" Value="{Binding Path=CanBeSelected}" />
            <Setter Property="MouseDoubleClickCommand" Value="{Binding Path=SelectAndDoneCommand}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <dxe:TextEdit NullText="Type to search..." EditValue="{Binding Path=FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,3" />
        <local:DesignTimeTreeView Grid.Row="1" ItemsSource="{Binding Path=RootTreeNodeViewModel.Children}" ItemContainerStyle="{StaticResource ItemTreeViewItemStyle}">
            <local:DesignTimeTreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="16" Height="16" Stretch="None" Margin="0,0,4,0" Source="{Binding Path=Icon}" />
                        <TextBlock Text="{Binding Path=Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </local:DesignTimeTreeView.ItemTemplate>
        </local:DesignTimeTreeView>
        <TextBlock Grid.Row="1" Text="Loading..." FontStyle="Italic" Opacity="0.75" Margin="6,2" Visibility="{Binding Path=Loading, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
