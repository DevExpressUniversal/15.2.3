<UserControl x:Class="DevExpress.Xpf.Scheduler.UI.ManageRecurrentAppointmentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"    
    xmlns:dxsch="clr-namespace:DevExpress.Xpf.Scheduler"
    xmlns:dxschcore="clr-namespace:DevExpress.XtraScheduler;assembly=DevExpress.XtraScheduler.v15.2.Core"
    xmlns:dxschd="clr-namespace:DevExpress.Xpf.Scheduler.Drawing"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid MinWidth="300" Margin="16,16,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Name="image1" VerticalAlignment="Top" Stretch="None" Source="{Binding ImageSource}" />
        <TextBlock x:Name="messageTextBlock" AutomationProperties.HelpText="{Binding Message}" Text="{Binding Message}" VerticalAlignment="Center" MaxWidth="250" Grid.Column="1" TextWrapping="Wrap" />
        <Grid Grid.Column="1" Grid.Row="1" Margin="0,11,0,0">
            <dxe:ListBoxEdit x:Name="actionsList" ShowBorder="False" Background="{x:Null}" DisplayMember="Name" 
                            EditValue="{Binding SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            ItemsSource="{Binding ActionsItemsSource}" VerticalAlignment="Top">
                <dxe:ListBoxEdit.StyleSettings>
                    <dxe:RadioListBoxEditStyleSettings />
                </dxe:ListBoxEdit.StyleSettings>
            </dxe:ListBoxEdit>
        </Grid>
    </Grid>
</UserControl>
