<dxschui:RemindersFormBase x:Class="DevExpress.Xpf.Scheduler.UI.RemindersForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxsch="clr-namespace:DevExpress.Xpf.Scheduler"
             xmlns:dxschd="clr-namespace:DevExpress.Xpf.Scheduler.Drawing"
             xmlns:dxschui="clr-namespace:DevExpress.Xpf.Scheduler.UI"
             mc:Ignorable="d" 
             Margin="10"
             Height="264" Width="490" x:Name="ReminderForm"
             dxsch:SchedulerFormBehavior.Title="{Binding Title}">    
    <UserControl.Resources>
        <dxsch:SchedulerControlStringIdConverter x:Key="SchedulerControlStringIdConverter"/>
    </UserControl.Resources>
    <Grid x:Name="VisualStatesRoot" MinWidth="270">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="MoreThanOneReminderSelectedState"/>
                <VisualState x:Name="NoRemindersSelectedState"/>
                <VisualState x:Name="OneReminderSelectedState">
                    <Storyboard>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="lblSubject">
                            <DiscreteDoubleKeyFrame KeyTime="0" Value="1"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="AUTO"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
            <TextBlock x:Name="lblSubject" FontWeight="Bold" Text="{Binding ElementName=lbReminders, Path=SelectedItem.Subject}" Margin="0,0,0,5" Opacity="0"/>
            <TextBlock Text="{Binding AppointmentStartDisplayText}"/>
            <!--  -->
        </StackPanel>
        <dxe:ListBoxEdit x:Name="lbReminders"
                         Margin="0,5,0,5"
                         Grid.Row="1"
                         SelectionMode="Extended"
                         ItemsSource="{Binding Alerts, ElementName=ReminderForm}"
                         MinHeight="130"
                         SelectedIndex="0" TabIndex="0"
                         DisplayMember="Subject"
            >
        </dxe:ListBoxEdit>
        <StackPanel Grid.Row="2" HorizontalAlignment="Stretch" Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="AUTO"/>
                    <ColumnDefinition Width="AUTO"/>
                </Grid.ColumnDefinitions>
                <StackPanel HorizontalAlignment="Left" MinWidth="85">
                    <Button MinWidth="75" Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_DismissAll, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="DismissAll_Click" TabIndex="1" />
                </StackPanel>
                <Button Grid.Column="1" x:Name="btnOpen" MinWidth="75" Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_OpenItem, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="OpenItem_Click" TabIndex="2" IsEnabled="{Binding CanOpenEditAppointmentForm}"/>
                <Button x:Name="btnDismiss" Grid.Column="2" MinWidth="75" Margin="10,0,0,0" Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Dismiss, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="Dismiss_Click" TabIndex="3" />
            </Grid>
            <TextBlock Margin="0,5,0,5" Text="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=Form_ClickSnoozeToBeReminderAgainIn, Converter={StaticResource SchedulerControlStringIdConverter}}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="AUTO"/>
                </Grid.ColumnDefinitions>
                <dxe:ComboBoxEdit x:Name="Snooze"
                                  AutomationProperties.Name="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=Form_ClickSnoozeToBeReminderAgainIn, Converter={StaticResource SchedulerControlStringIdConverter}}"
                                  DisplayMember="DisplayText"
                                  ValueMember="TimeSpan"
                                  IsTextEditable="False"
                                  ItemsSource="{Binding SnoozeList}"
                                  EditValue="{Binding ElementName=ReminderForm, Path=SnoozeValue, Mode=TwoWay}" TabIndex="4">                                  
                </dxe:ComboBoxEdit>
                <Button x:Name="btnSnooze" Grid.Column="1" MinWidth="75" Margin="10,0,0,0" Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Snooze, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="Snooze_Click" TabIndex="5" />
            </Grid>
        </StackPanel>
    </Grid>
</dxschui:RemindersFormBase>
