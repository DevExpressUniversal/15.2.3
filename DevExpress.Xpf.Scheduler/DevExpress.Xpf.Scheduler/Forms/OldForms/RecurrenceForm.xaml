<UserControl x:Class="DevExpress.Xpf.Scheduler.UI.RecurrenceForm" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
    xmlns:dxsch="clr-namespace:DevExpress.Xpf.Scheduler" 
    xmlns:dxschdui="clr-namespace:DevExpress.Xpf.Scheduler.UI" 
    xmlns:dxschd="clr-namespace:DevExpress.Xpf.Scheduler.Drawing" 
    xmlns:dxec="clr-namespace:DevExpress.Xpf.Editors.Controls;assembly=DevExpress.Xpf.Core.v15.2"
    x:Name="RecForm" 
    dxsch:SchedulerFormBehavior.Title="{Binding Title}"
    MinWidth="550">
    <UserControl.Resources>
        <dxsch:SchedulerStringIdConverter x:Key="SchedulerStringIdConverter" />
        <dxsch:SchedulerControlStringIdConverter x:Key="SchedulerControlStringIdConverter" />
        <dxschd:TimeSpanToDateTimeConverter x:Key="timeSpanToDateTimeConverter" />
        <dxschd:InvertedBoolConverter x:Key="invertedBoolConverter" />
        <dx:EnumToBoolConverter x:Key="EnumToBoolConverter" />
        <dx:BoolToVisibilityConverter x:Key="booleanToVisibilityConverter" />
        <dx:BoolToVisibilityInverseConverter x:Key="invertedBooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <dx:GroupFrame Header="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=Form_RecurrencePattern, Converter={StaticResource SchedulerControlStringIdConverter}}" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <dxschdui:RecurrenceTypeControl Controller="{Binding RecurrenceVisualController}" IsReadOnly="{Binding ReadOnly}" />
                <dxec:DXSeparator Grid.Column="1" Orientation="Vertical" Margin="10,0,14,0" UseLayoutRounding="True" />
                <StackPanel Grid.Column="2">
                    <dxschdui:DailyRecurrenceControl Visibility="{Binding RecurrenceVisualController.IsDailyRecurrence, Converter={dxschd:BoolToVisibilityConverter}}" RecurrenceInfo="{Binding RecurrenceVisualController.RecurrenceInfo}" IsReadOnly="{Binding ReadOnly}" />
                    <dxschdui:WeeklyRecurrenceControl Visibility="{Binding RecurrenceVisualController.IsWeeklyRecurrence, Converter={dxschd:BoolToVisibilityConverter}}" RecurrenceInfo="{Binding RecurrenceVisualController.RecurrenceInfo}" IsReadOnly="{Binding ReadOnly}" FirstDayOfWeek="{Binding FirstDayOfWeek}" />
                    <dxschdui:MonthlyRecurrenceControl Visibility="{Binding RecurrenceVisualController.IsMonthlyRecurrence, Converter={dxschd:BoolToVisibilityConverter}}" RecurrenceInfo="{Binding RecurrenceVisualController.RecurrenceInfo}" IsReadOnly="{Binding ReadOnly}" />
                    <dxschdui:YearlyRecurrenceControl Visibility="{Binding RecurrenceVisualController.IsYearlyRecurrence, Converter={dxschd:BoolToVisibilityConverter}}" RecurrenceInfo="{Binding RecurrenceVisualController.RecurrenceInfo}" IsReadOnly="{Binding ReadOnly}" />
                </StackPanel>
            </Grid>
        </dx:GroupFrame>
        <dx:GroupFrame Grid.Row="1" Header="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=Form_RangeOfRecurrence, Converter={StaticResource SchedulerControlStringIdConverter}}" Margin="0,12,0,0">
            <dxschdui:RecurrenceRangeControl RecurrenceInfo="{Binding RecurrenceVisualController.RecurrenceInfo}"
                        TimeZoneHelper="{Binding TimeZoneHelper, Mode=OneTime}"
                        Pattern="{Binding RecurrenceVisualController.Controller.PatternCopy}" IsReadOnly="{Binding ReadOnly}" 
                        />
        </dx:GroupFrame>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="OkButton" AutomationProperties.Name="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_OK, Converter={StaticResource SchedulerControlStringIdConverter}}"
                Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_OK, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="OnOKButtonClick" MinWidth="75" Margin="6,0,0,0" IsEnabled="{Binding ReadOnly, Converter={dxschd:InvertedBoolConverter}}" />
            <Button AutomationProperties.Name="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Cancel, Converter={StaticResource SchedulerControlStringIdConverter}}"
                Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Cancel, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="OnCancelButtonClick" MinWidth="75" Margin="6,0,0,0" />
            <Button AutomationProperties.Name="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Delete, Converter={StaticResource SchedulerControlStringIdConverter}}"
                Content="{Binding Source={StaticResource SchedulerControlStringIdConverter}, ConverterParameter=ButtonCaption_Delete, Converter={StaticResource SchedulerControlStringIdConverter}}" Click="OnDeleteButtonClick" MinWidth="75" Margin="6,0,0,0" IsEnabled="{Binding ReadOnly, Converter={dxschd:InvertedBoolConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
