<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DevExpress.Charts.Designer.Native"
    xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/charts">

    <Style
        TargetType="{x:Type local:DesignChartControl}">
        <Setter
            Property="Template">
            <Setter.Value>
                <ControlTemplate
                    TargetType="{x:Type local:DesignChartControl}">
                    <Grid>
                        <ContentPresenter
                            Content="{Binding Path=ChartControl, RelativeSource={RelativeSource TemplatedParent}}">
                            <ContentPresenter.InputBindings>
                                <KeyBinding
                                    Key="Delete"
                                    Command="{x:Static local:ChartDesignerWindow.DeleteElementChartCommand}" />
                            </ContentPresenter.InputBindings>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
