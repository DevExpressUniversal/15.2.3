<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    xmlns:local="clr-namespace:DevExpress.Xpf.DemoBase.Helpers"
                    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm">
    <Style TargetType="{x:Type local:ColumnScrollControl}">
        <Style.Resources>
            <Style x:Key="EmptyButton" TargetType="Button">
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid Background="Transparent">
                                <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="IsTabStop" Value="False" />
                <Setter Property="Focusable" Value="False" />
            </Style>
            <dxmvvm:ObjectToObjectConverter x:Key="PageButtonOpacityConverter">
                <dxmvvm:MapItem Source="false" Target="1"/>
                <dxmvvm:MapItem Source="true" Target="0.15"/>
            </dxmvvm:ObjectToObjectConverter>
            <dxmvvm:ObjectToObjectConverter x:Key="OpacityConverter">
                <dxmvvm:MapItem Source="False" Target="0.35" />
                <dxmvvm:MapItem Source="True" Target="1" />
            </dxmvvm:ObjectToObjectConverter>
            <dxmvvm:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <dxmvvm:BooleanToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" Inverse="True"/>
            <local:StringFormatToUriConverter x:Key="StringFormatToUriConverter"/>
            <local:ColumnScrollPanelVisibilityConverter x:Key="ColumnScrollPanelVisibilityConverter"/>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ColumnScrollControl}">
                    <DockPanel x:Name="rootPanel" VerticalAlignment="Stretch" Background="Transparent">
                        <Button x:Name="prevButton" Style="{StaticResource EmptyButton}"
                                DockPanel.Dock="Left"
                                VerticalAlignment="Stretch"
                                VerticalContentAlignment="Center"
                                Margin="0,2,2,0"
                                Opacity="0"
                                Visibility="{TemplateBinding PageButtons, Converter={StaticResource ColumnScrollPanelVisibilityConverter}}">
                            <Image Source="pack://application:,,,/DevExpress.Xpf.DemoBase.v15.2;component/Images/Prev.png"
                                   Opacity="{TemplateBinding IsFirstPageSelected, Converter={StaticResource PageButtonOpacityConverter}}"
                                   Stretch="None"/>
                        </Button>
                        <Button x:Name="nextButton"
                                Style="{StaticResource EmptyButton}"
                                DockPanel.Dock="Right"
                                VerticalAlignment="Stretch"
                                VerticalContentAlignment="Center"
                                Margin="2,2,0,0"
                                Opacity="0">
                            <Image Source="pack://application:,,,/DevExpress.Xpf.DemoBase.v15.2;component/Images/Next.png"
                                   Opacity="{TemplateBinding IsLastPageSelected, Converter={StaticResource PageButtonOpacityConverter}}"
                                   Stretch="None"
                                   Visibility="{TemplateBinding PageButtons, Converter={StaticResource ColumnScrollPanelVisibilityConverter}}"/>
                        </Button>
                        <local:ColumnScrollPanel x:Name="panel">
                            <dx:DXBorder x:Name="scrollViewerContainer">
                                <dx:DXBorder.OpacityMask>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0.03,0">
                                        <GradientStop Color="Transparent" Offset="0"/>
                                        <GradientStop x:Name="leftOpacityGradient" Color="Black" Offset="0"/>
                                    </LinearGradientBrush>
                                </dx:DXBorder.OpacityMask>
                                <ScrollViewer x:Name="scrollViewer" Focusable="False" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden">
                                    <ScrollViewer.OpacityMask>
                                        <LinearGradientBrush StartPoint="0.97,0" EndPoint="1,0">
                                            <GradientStop x:Name="opacityGradient" Color="Black" Offset="1"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </ScrollViewer.OpacityMask>
                                    <StackPanel Orientation="Horizontal">
                                        <dx:DXBorder Width="10000"/>
                                        <ContentPresenter/>
                                        <dx:DXBorder Width="10000"/>
                                    </StackPanel>
                                </ScrollViewer>
                            </dx:DXBorder>
                            <ItemsControl x:Name="pageButtonsControl"
                                      Focusable="False"
                                      ItemsSource="{TemplateBinding PageButtons}"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,0"
                                      Visibility="{TemplateBinding PageButtons, Converter={StaticResource ColumnScrollPanelVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <RadioButton IsChecked="{Binding IsChecked, Mode=TwoWay}" Focusable="False">
                                            <RadioButton.Template>
                                                <ControlTemplate TargetType="{x:Type RadioButton}">
                                                    <Grid Margin="2,0" Background="Transparent">
                                                        <Ellipse x:Name="optionMark" Stroke="#838383" MinWidth="8" MinHeight="8" Opacity="1"/>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="true">
                                                            <Setter TargetName="optionMark" Property="Fill" Value="#838383"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RadioButton.Template>
                                        </RadioButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </local:ColumnScrollPanel>
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsOpacityMaskVisible, RelativeSource={RelativeSource Self}}" Value="true">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="opacityGradient"
                                                         Storyboard.TargetProperty="Offset"
                                                         To="0" Duration="0:0:0.4">
                                            <DoubleAnimation.EasingFunction>
                                                <QuinticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="opacityGradient"
                                                         Storyboard.TargetProperty="Offset"
                                                         To="1" Duration="0:0:0.4">
                                            <DoubleAnimation.EasingFunction>
                                                <QuinticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsLeftOpacityMaskVisible, RelativeSource={RelativeSource Self}}" Value="true">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="leftOpacityGradient"
                                                         Storyboard.TargetProperty="Offset"
                                                         To="1" Duration="0:0:0.4">
                                            <DoubleAnimation.EasingFunction>
                                                <QuinticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="leftOpacityGradient"
                                                         Storyboard.TargetProperty="Offset"
                                                         To="0" Duration="0:0:0.4">
                                            <DoubleAnimation.EasingFunction>
                                                <QuinticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="prevButton"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.4"/>
                                        <DoubleAnimation Storyboard.TargetName="nextButton"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.4"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="prevButton"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.4"/>
                                        <DoubleAnimation Storyboard.TargetName="nextButton"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.4"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>