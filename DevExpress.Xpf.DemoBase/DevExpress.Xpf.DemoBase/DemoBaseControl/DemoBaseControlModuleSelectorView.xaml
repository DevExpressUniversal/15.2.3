<internal:DemoBaseControlPartView x:Class="DevExpress.Xpf.DemoBase.Internal.DemoBaseControlModuleSelectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ddhelpers="clr-namespace:DevExpress.DemoData.Helpers;assembly=DevExpress.DemoData.v15.2.Core"
    xmlns:helpers="clr-namespace:DevExpress.Xpf.DemoBase.Helpers"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:internal="clr-namespace:DevExpress.Xpf.DemoBase.Internal"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:dxdb="clr-namespace:DevExpress.Xpf.DemoBase"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300">

    <internal:DemoBaseControlPartView.Template>
        <ControlTemplate TargetType="{x:Type UserControl}">
            <dx:DXBorder BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </dx:DXBorder>
        </ControlTemplate>
    </internal:DemoBaseControlPartView.Template>
    
    <Grid>
        <Grid.Resources>
            <dxmvvm:ObjectToObjectConverter x:Key="VisibleGroupsCountToVisibilityConverter" DefaultTarget="Collapsed">
                <dxmvvm:MapItem Source="0" Target="Visible"/>
            </dxmvvm:ObjectToObjectConverter>
            <TextDecorationCollection x:Key="LinkDecorations">
                <TextDecoration PenThicknessUnit="Pixel" PenOffsetUnit="Pixel">
                    <TextDecoration.Pen>
                        <Pen Thickness="1" DashCap="Flat">
                            <Pen.Brush>
                                <SolidColorBrush Color="#26A4DD"/>
                            </Pen.Brush>
                            <Pen.DashStyle>
                                <DashStyle Dashes="2,2"/>
                            </Pen.DashStyle>
                        </Pen>
                    </TextDecoration.Pen>
                </TextDecoration>
            </TextDecorationCollection>
        </Grid.Resources>
        <TextBlock Visibility="{Binding VisibleGroupsCount, ElementName=demoLinks, Converter={StaticResource VisibleGroupsCountToVisibilityConverter}}"
                   Foreground="#A3A3A3"
                   FontSize="32"
                   FontFamily="Segoe UI Light"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,0,0,66">
                Unable to locate a demo matching your keyword.<LineBreak/>
                Please enter a different keyword and try again.
        </TextBlock>
        <internal:ModuleSelectorScrollViewer dx:ScrollBarExtensions.ScrollBarMode="TouchOverlap"
                  Margin="24,0,25,0"
                  Focusable="False"
                  HorizontalAlignment="Left"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Disabled">
            <dxdb:GroupedLinksControl x:Name="demoLinks" Focusable="False" SearchId="{Binding Product.Name}" FilterString="{Binding FilterText, Mode=TwoWay}" LayoutType="Wrapped" ItemsSource="{Binding Modules}" Padding="0,38,0,30" LinkSelectedCommand="{Binding LinkSelectedCommand}">
                <dxmvvm:Interaction.Behaviors>
                    <internal:GroupedLinksControlService/>
                </dxmvvm:Interaction.Behaviors>
                <dxdb:GroupedLinksControl.GroupHeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Header}" FontSize="12" FontFamily="Segoe UI Bold" Foreground="#9D9D9D" Margin="28,0,4,4"/>
                    </DataTemplate>
                </dxdb:GroupedLinksControl.GroupHeaderTemplate>
                <dxdb:GroupedLinksControl.LinkTemplate>
                    <DataTemplate>
                        <dx:DXBorder Background="Transparent">
                            <StackPanel Orientation="Horizontal" Margin="0,0,4,4">
                                <StackPanel Width="28" HorizontalAlignment="Right" Orientation="Horizontal" FlowDirection="RightToLeft">
                                    <Border x:Name="highlightedStatus" Margin="5,2,0,0" Width="9" Height="9" Visibility="Collapsed" Background="#4271BF"/>
                                    <Border x:Name="newStatus" Margin="5,2,0,0" Width="9" Height="9" Visibility="Collapsed" Background="#F74710"/>
                                    <Border x:Name="updatedStatus" Margin="5,2,0,0" Width="9" Height="9" Visibility="Collapsed" Background="#57C82B" />
                                </StackPanel>
                                <TextBlock x:Name="tb" Text="{Binding Title}"/>
                            </StackPanel>
                        </dx:DXBorder>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tb" Property="TextDecorations" Value="{StaticResource LinkDecorations}"/>
                                <Setter TargetName="tb" Property="Foreground" Value="#26A4DD"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsNew}" Value="True">
                                <Setter TargetName="newStatus" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsUpdated}" Value="True">
                                <Setter TargetName="updatedStatus" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                <Setter TargetName="highlightedStatus" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </dxdb:GroupedLinksControl.LinkTemplate>
            </dxdb:GroupedLinksControl>
        </internal:ModuleSelectorScrollViewer>
    </Grid>
</internal:DemoBaseControlPartView>
