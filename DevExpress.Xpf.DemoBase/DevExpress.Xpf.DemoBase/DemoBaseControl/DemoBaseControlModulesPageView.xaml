<internal:DemoBaseControlPartView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ddhelpers="clr-namespace:DevExpress.DemoData.Helpers;assembly=DevExpress.DemoData.v15.2.Core"
    xmlns:localddhelpers="clr-namespace:DevExpress.DemoData.Helpers"
    xmlns:helpers="clr-namespace:DevExpress.Xpf.DemoBase.Helpers"
    xmlns:internal="clr-namespace:DevExpress.Xpf.DemoBase.Internal"
    xmlns:dxdb="clr-namespace:DevExpress.Xpf.DemoBase"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    x:Class="DevExpress.Xpf.DemoBase.Internal.DemoBaseControlModulesPageView"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300">

    <internal:DemoBaseControlPartView.Template>
        <ControlTemplate TargetType="{x:Type UserControl}">
            <dx:DXBorder BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </dx:DXBorder>
        </ControlTemplate>
    </internal:DemoBaseControlPartView.Template>
    
    <internal:DemoBaseControlPartView.Resources>
        <ddhelpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style x:Key="EmptyTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Padding="{TemplateBinding Padding}" Foreground="{TemplateBinding Foreground}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ItemsPanelTemplate x:Key="DemosPanelTemplate">
            <StackPanel />
        </ItemsPanelTemplate>
        <DataTemplate x:Key="DemoTemplate">
            <TextBlock Text="{Binding Title.TextAsync}" />
        </DataTemplate>
        <Style x:Key="WhiteLinkStyle" TargetType="TextBlock">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="TextDecorations">
                <Setter.Value>
                    <TextDecorationCollection>
                        <TextDecoration PenThicknessUnit="Pixel" PenOffsetUnit="Pixel">
                            <TextDecoration.Pen>
                                <Pen Thickness="1">
                                    <Pen.Brush>
                                        <SolidColorBrush Color="White"/>
                                    </Pen.Brush>
                                </Pen>
                            </TextDecoration.Pen>
                        </TextDecoration>
                    </TextDecorationCollection>
                </Setter.Value>
            </Setter>
        </Style>
    </internal:DemoBaseControlPartView.Resources>

    <DockPanel Margin="0,30,0,0" MouseUp="OnClearFilterFocus">
        <helpers:DemoScreenFooterControl DockPanel.Dock="Bottom" PlatformName="Wpf"/>
        <Grid Height="64" Background="#26A4DD" DockPanel.Dock="Bottom" Margin="1,0">
            <StackPanel Orientation="Horizontal" Margin="46,3,0,0">
                <Image Source="pack://application:,,,/DevExpress.Xpf.DemoBase.v15.2;component/Images/CheckOnlineExamples.png" Stretch="None"/>
                <TextBlock Text="Explore our online samples" Margin="11,20,0,0" VerticalAlignment="Top" Style="{StaticResource WhiteLinkStyle}">
                    <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand EventName="MouseUp" Command="{Binding CheckExamplesCommand}"/>
                    </dxmvvm:Interaction.Behaviors>
                </TextBlock>
            </StackPanel>
            <ItemsControl Focusable="False" ItemsSource="{Binding CurrentProduct.Solutions}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,40,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="30,0,0,0" Text="{Binding Title}" Style="{StaticResource WhiteLinkStyle}">
                            <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand EventName="MouseUp" Command="{Binding OpenCommand}"/>
                    </dxmvvm:Interaction.Behaviors>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <helpers:DraggableBorder DockPanel.Dock="Top">
            <helpers:FilterLineControl x:Name="filter" FilterText="{Binding FilterText, Mode=TwoWay}" ClearFocus="Filter_OnClearFocus" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,12,0,33"/>
        </helpers:DraggableBorder>
        <dx:DXBorder Height="57" DockPanel.Dock="Top" Margin="1,0,1,0" BorderThickness="0,1,0,0" BorderBrush="#D9D9D9" Background="#F2F2F2">
            <StackPanel Margin="37,1,0,0" Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type CheckBox}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type CheckBox}">
                                    <Grid Width="133" Height="27">
                                        <dx:DXBorder Background="Transparent"/>
                                        <StackPanel Orientation="Horizontal">
                                            <Border Height="9" Width="9" Margin="13,0,6,0" Background="{Binding Content[Brush], RelativeSource={RelativeSource TemplatedParent}}"/>
                                            <TextBlock Margin="0,5,0,0" Text="{Binding Content[Title], RelativeSource={RelativeSource TemplatedParent}}" Foreground="Black"/>
                                        </StackPanel>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>
                <CheckBox>
                    <ResourceDictionary>
                        <sys:String x:Key="Title">New Demo</sys:String>
                        <SolidColorBrush x:Key="Brush">#F74710</SolidColorBrush>
                    </ResourceDictionary>
                </CheckBox>
                <CheckBox Margin="20,0">
                    <ResourceDictionary>
                        <sys:String x:Key="Title">Updated Demo</sys:String>
                        <SolidColorBrush x:Key="Brush">#57C82B</SolidColorBrush>
                    </ResourceDictionary>
                </CheckBox>
                <CheckBox>
                    <ResourceDictionary>
                        <sys:String x:Key="Title">Featured Demo</sys:String>
                        <SolidColorBrush x:Key="Brush">#4271BF</SolidColorBrush>
                    </ResourceDictionary>
                </CheckBox>
            </StackPanel>
        </dx:DXBorder>
        <localddhelpers:ViewPresenter Content="{Binding ModuleSelector.View}" DelayContentLoad="500" UseAnimation="{Binding UseAnimation}" Storyboard="Animation" ActualContentChangedCommand="{Binding RaiseActualProductChangedCommand}">
            <localddhelpers:ViewPresenter.Resources>
                <Storyboard x:Key="Animation">
                    <DoubleAnimation From="1" To="0" Duration="00:00:00.50" Storyboard.TargetProperty="(localddhelpers:ViewPresenter.NewContentTranslateX)" />
                    <DoubleAnimation From="0" To="-1" Duration="00:00:00.50" Storyboard.TargetProperty="(localddhelpers:ViewPresenter.OldContentTranslateX)" />
                    <DoubleAnimation To="1" Duration="00:00:00.50" Storyboard.TargetProperty="(localddhelpers:ViewPresenter.NewContent).(UIElement.Opacity)" />
                    <DoubleAnimation To="0" Duration="00:00:00.50" Storyboard.TargetProperty="(localddhelpers:ViewPresenter.OldContent).(UIElement.Opacity)" />
                </Storyboard>
            </localddhelpers:ViewPresenter.Resources>
        </localddhelpers:ViewPresenter>
    </DockPanel>
</internal:DemoBaseControlPartView>
