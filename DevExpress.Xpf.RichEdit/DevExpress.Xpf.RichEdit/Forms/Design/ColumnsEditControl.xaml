<UserControl x:Class="DevExpress.Xpf.RichEdit.UI.ColumnsEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxre="clr-namespace:DevExpress.Xpf.RichEdit"
             xmlns:dxreui="clr-namespace:DevExpress.Xpf.RichEdit.UI"
             dx:StyleManager.ApplyApplicationTheme="True">

    <UserControl.Resources>
        <dxre:RichEditStringIdConverter x:Key="RichEditStringIdConverter" />
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="panel">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="30" />
                    <ColumnDefinition Width="Auto" MinWidth="60" />
                    <ColumnDefinition Width="Auto" MinWidth="60" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Margin="0,2,2,2" Text="{Binding Source={StaticResource RichEditStringIdConverter}, ConverterParameter=Caption_ColumnsSetupFormColumnNumberLabel, Converter={StaticResource RichEditStringIdConverter}}" />
                <TextBlock Grid.Column="1" Margin="2" Text="{Binding Source={StaticResource RichEditStringIdConverter}, ConverterParameter=Caption_ColumnsSetupFormColumnWidthLabel, Converter={StaticResource RichEditStringIdConverter}}" />
                <TextBlock Grid.Column="2" Margin="2" Text="{Binding Source={StaticResource RichEditStringIdConverter}, ConverterParameter=Caption_ColumnsSetupFormColumnSpacingLabel, Converter={StaticResource RichEditStringIdConverter}}" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
